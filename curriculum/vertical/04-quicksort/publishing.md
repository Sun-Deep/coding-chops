# Publishing

Posted. `script.md` is `Status: published` and the understanding check is
complete.

## Files

```text
reel     out/vertical/vr04-quicksort.mp4    1080x1920, 805 frames, 26.8s
cover    out/vertical/vr04-cover.png        1080x1920
```

## Facebook, Instagram, TikTok caption

```text
200 numbers cost selection sort 19,900 comparisons. An already sorted list costs exactly the same.

The count is n(n-1)/2. No early exit, so it never learns it has nothing to do. Quicksort does the same array in 1,554.

The catch is writes. Quicksort moves 1,228 values against 384. On flash, where writes wear the part out, the slow one wins.

Code and measurements: github.com/Sun-Deep/coding-chops

#quicksort #algorithms #datastructures #backend #programming
```

Four hundred and sixty-two characters. The finding closes at ninety-seven, so
the whole of it survives Facebook's cut before "more" rather than breaking
mid-word, which the first draft did at 569.

## YouTube Shorts title

```text
Selection sort does 19,900 comparisons even when sorted
```

Fifty-four characters, and the number lands at character twenty, inside the
forty a phone shows.

## Checklist

- [x] First 100 characters carry the surprise
- [x] Every figure in the caption is in `measurements.ts`
- [x] The catch is named
- [x] Five hashtags, none the channel name
- [x] Title under 60 characters, hook inside the first 40
- [x] No em dashes, no call to action, no question mark
- [x] Repository line present
