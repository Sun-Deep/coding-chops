# Understanding check

The creator gate. Answer each without the notes, the script output or the
composition. Tick only what you can actually do.

- [x] Derive 19,900 from two hundred elements without looking up the formula.
- [x] Explain why selection sort costs the same on a sorted list as on a
      shuffled one, without using the word "worst case".
- [x] Say what a partition pass does, and why the ranges halving is what makes
      quicksort cheap.
- [x] Explain why quicksort's comparison count moves with the input and
      selection sort's does not.
- [x] State the write counts for both and say where quicksort's extra writes
      come from.
- [x] Say which algorithm you would put in a flash controller, and why the
      answer is not the fast one.
- [x] Describe quicksort's worst case and say what input triggers it with a last
      element pivot.
- [x] Say what would change if the partition were Hoare's rather than Lomuto's.
