# Storyboard

Every teaching element is present at frame zero. There are no scene changes.

| Frames  | Motion                                                            |
| ------- | ----------------------------------------------------------------- |
| 0-419   | Five x-axes and lines extend together from one accelerating clock. |
| 0-420   | All five charts, complexity labels, and code lines stay visible.   |
| 0-419   | Each marker and measured operation count advances with input `n`.  |
| 419     | Every line reaches its endpoint together at `n = 1,024`.           |

Each card uses its own normalized work axis. This preserves the flat, concave, straight, and convex growth shapes without pretending the card heights share a numeric scale. Time changes the playback rate, not the graph geometry. Orange always means the current input position.

All readable material sits inside the vertical safe area. The four small cards end above 1,000. The wide fifth card stays left of the lower-right action rail. The on-screen narration baseline stays at 1,460.

Eight ticks divide the shared horizontal travel. A heavier impact lands as the lines reach the right edge. There is no music or spoken narration.
