# Publishing

Blocked until the creator understanding check passes. The render is ready for
review and not ready to upload.

## Files

```text
reel     out/vertical/vr12-file-modes.mp4               1080x1920, 420 frames, 14.06 seconds
cover    out/vertical/vr12-file-modes-cover.png         1080x1920
crops    out/vertical/vr12-file-modes-cover-crop-1x1.png
         out/vertical/vr12-file-modes-cover-crop-3x4.png
```

## Facebook, Instagram and TikTok caption

```text
chmod 755 is nine switches. Read is 4, write is 2, run is 1, and the digit is the sum.

On a folder, x means enter, not run: a 644 folder hides a 644 file.

Code and measurements: github.com/Sun-Deep/coding-chops

#chmod #linux #bash #devops #programming
```

254 characters. The first line is 86, so the finding survives the cut
Facebook puts at about 100.

## YouTube Shorts title

```text
chmod 755 is nine switches
```

26 characters, so the whole claim is inside the truncation.

## YouTube Shorts description

Same two lines as the caption.

```text
chmod 755 is nine switches. Read is 4, write is 2, run is 1, and the digit is the sum.

On a folder, x means enter, not run: a 644 folder hides a 644 file.

Code and measurements: github.com/Sun-Deep/coding-chops

#chmod #linux #bash #devops #programming
```

## Checks

- [x] The whole caption is 254 characters, hashtags included
- [x] Two lines of prose, no paragraph re-explaining the mechanism
- [x] The surprise is inside the first 100 characters
- [x] Every number in the caption is in `measurements.ts`
- [x] The catch is named, in one sentence, and it was run rather than asserted
- [x] Five hashtags, lowercase, none of them the channel name
- [x] Title under 60 characters with the hook inside the first 40
- [x] No em dashes, no call to action, no question mark
- [x] The repository line is there
