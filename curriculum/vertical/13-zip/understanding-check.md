# Understanding check

The creator gate. Answer each without the notes, the script or the render.

- [ ] Say what LZ77 writes down when it finds a repeat, and what the two numbers in that pointer mean.
- [ ] Explain why the first line of the file is never compressed, and what that says about how the encoder works.
- [ ] Say why a match is allowed to overlap the text it points back at, and what that encodes efficiently.
- [ ] Explain the difference between the 939 bytes of copies and the 858 bytes saved, and why the reel never subtracts one from the other.
- [ ] Say what the `Z_HUFFMAN_ONLY` run is for, and what it would have meant if it had come back near 177.
- [ ] Explain why this file's parse is called a demonstration rather than a measurement, and which number on screen is exact.
