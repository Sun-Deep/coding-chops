/**
 * Channel copy that appears on brand assets rather than inside a lesson.
 *
 * Picture is doing two jobs at once. It is the drawing on screen, and it is
 * what the viewer walks away holding in their head. That is the whole
 * transaction, and it reads the same for a load balancer, a binary tree and an
 * array of stock prices, so all three tracks sit under one line.
 *
 * Earlier attempts described the creator's standard rather than the viewer's
 * side of it: "If you can't draw it, you don't know it" and "Drawn from
 * memory". Both were rules for the person making the lesson. A tagline is a
 * promise to the person watching it.
 *
 * Stored without a trailing period. Banners set it uppercase with wide
 * tracking, where a period reads as a typo.
 */
export const TAGLINE = "Ideas you can picture";

/**
 * One line for a bio or an about field, where the tagline cannot carry the
 * subjects as well. Fits inside a 160 character bio.
 */
export const DESCRIPTOR =
  "Concepts taken apart and drawn, one lesson at a time. " +
  "System design, data structures and algorithms, and problem solving.";

/** The publishing gate, spelled out. For an about page, never for a banner. */
export const STANDARD =
  "A lesson ships when I can explain it, draw it and apply it with the script closed.";
