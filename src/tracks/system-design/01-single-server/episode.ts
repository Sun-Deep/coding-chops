import { seconds } from "../../../shared/video/timing";

export const episode01 = {
  id: "sd-01-single-server",
  title: "How a Web Application Works on One Server",
  status: "visual-prototype",
  durations: {
    master: seconds(30),
    short: seconds(20),
  },
} as const;
